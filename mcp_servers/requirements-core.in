# CORE RUNTIME DEPENDENCIES
# Shared by >80% of fleet services.
# These libraries define the minimum runtime environment for any Sanctuary MCP server.

fastapi
uvicorn
sse-starlette
httpx
pydantic
fastmcp  # MCP SDK – assumed baseline for all Sanctuary services
tiktoken

# SECURITY BASELINES (GH Alerts Jan 2026)
urllib3>=2.2.2
aiohttp>=3.9.4

# SECURITY PATCHES (Feb 2026)
python-multipart>=0.0.22
cryptography>=46.0.5

# ACKNOWLEDGED ADVISORY (Feb 2026) - no fix available
# diskcache==5.6.3 (latest) - inherent pickle deserialization risk (transitive via py-key-value-aio)
