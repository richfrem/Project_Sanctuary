sequenceDiagram
    participant User as User / Agent
    participant CLI as task_manager.py
    participant JSON as tasks/tasks.json

    User->>CLI: create "Fix bug" --objective "..."
    CLI->>JSON: Append task (id=auto, status=todo)
    CLI-->>User: ✅ Created #N

    User->>CLI: board
    CLI->>JSON: Read all tasks
    CLI-->>User: 📋 Kanban board (4 lanes)

    User->>CLI: move N in-progress --note "Starting"
    CLI->>JSON: Update status + add note
    CLI-->>User: ✅ Moved #N: todo → in-progress

    User->>CLI: list --status done
    CLI->>JSON: Filter by status
    CLI-->>User: List of completed tasks
