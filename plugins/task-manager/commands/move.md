---
description: Move a task between kanban lanes
argument-hint: "<task_id> <backlog|todo|in-progress|done> [--note reason]"
---

# Move Task

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/task_manager.py move 3 in-progress --note "Starting work"
```
