# MCP Migration Configuration
# Controls the behavior of Git integrity and operational logic in the canonical MCP architecture.
# Based on Protocol 101 v3.0 (Absolute Stability - Functional Coherence).

# Enable MCP-specific operational logic
MCP_ENABLED=true

# VALIDATE_COMMAND_JSON
# Controls external validation of the command.json structure. Set to 'true' to enable strict validation.
VALIDATE_COMMAND_JSON=false
